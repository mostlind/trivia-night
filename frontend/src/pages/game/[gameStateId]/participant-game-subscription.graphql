subscription ParticipantGame($gameStateId: uuid!) {
  game_state_by_pk(id: $gameStateId) {
    state
    game {
      name
    }
    teams {
      name
    }
    current_question {
      state
      question {
        question_text
      }
      answers {
        value
      }
    }
  }
}
